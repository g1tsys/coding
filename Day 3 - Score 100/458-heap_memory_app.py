"""
题目描述:
有一个总空间为100字节的堆，现要从中新申请一块内存，内存分配原则为优先紧接着前一块已使用内存分配空间足够且最接近申请大小的空闲内存。
输入输出:
输入
第1行是1个整数，表示期望申请的内存字节数;第2到N行是用空格分割的两个整数，表示当前已分配的内存的情况，每一行表示一块已分配的连续内存空间，每行的第1和第2个整数分别表示偏移地址和内存块大小，如:01
32
表示0偏移地址开始的1个字节和3偏移地址开始的2个字节已被分配，其余内存空闲
输出若申请成功，输出申请到内存的偏移;若申请失败，输出-1
补充说明:
1、若输入信息不合法或无效，则申请失败2、若没有足够的空间供分配，则申请失败
3、堆内存信息有区域重叠或有非法值等都是无效输入

1.我们首先读取第一行输入，得到期望申请的内存字节数requested_size。然后，我们读取接下来的每一行输入(直到空行或文件结束)，记录每个已分配内存块的起始偏移地址和大小，存储到列表allocatedblocks中。
2.请求的内存大小必须是一个合法值，即它应该大于0字节且不超过堆的总大小(在这个例子中是100字节)。如果请求的内存大小不在这个范围内，直接返回-1。
3.为了找到所有的空闲块，我们需要遍历已分配的内存块列表。我们从偏移地址0开始，检查每一对相邻的已分配内存块之间是否有足够的空间。如果两个相邻的内存块之间的空闲空间大于等于请求的大小，我们就记录下这个空闲块的起始偏移地址和大小，将它们添加到free_blocks列表中。
4.在构建空闲块列表之前，我们需要对已分配的内存块进行排序，确保它们是按照偏移地址升序排列的。这样可以帮助我们检查是否有重叠的内存块(这将是无效的输入)，并且便于我们按顺序查找空闲块。
5.有了空闲块列表后，我们根据分配原则寻找第一个空闲块，它的大小足够大且最接近请求的大小。我们遍历free_blocks列表，返回第一个满足条件的空闲块的起始偏移地址。
6.我们还需要考虑堆内存的最后一部分。如果最后一个已分配的内存块之后有足够的空间，我们也应该将这部分视为一个空闲块。
7.如果找到了合适的空闲块，我们返回它的起始偏移地址。如果没有找到或输入无效，我们返回
1。

针对提供的样例1:
，期望申请的内存字节数是1。
，已分配的内存块有两个:从0开始的1字节和从3开始的2字节。
空闲的内存块有两个:从1开始的2字节和从5开始的95字节。
，第一个空闲块(从1开始的2字节)足够大且是最接近申请大小的，所以我们选择这个块进行分配，返回它的起始偏移地址1。
"""

#定义函数来处理内存分配
def allocate_memory(requested_size, allocated_blocks):#堆的总大小
    heap_size = 100
    #检查申请的内存是否大于0且小于等于堆大小
    
    if requested_size <= 0 or requested_size > heap_size:
        return -1
    #初始化空闲块列表
    free_blocks = []
    
    #堆的起始位置为0
    start = 0
    #对已分配的内存块进行排序
    allocated_blocks.sort()